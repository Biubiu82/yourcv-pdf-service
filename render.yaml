services:
  - type: web
    name: yourcv-pdf-service
    runtime: docker
    plan: free
    envVars:
      - key: PDF_API_KEY
        sync: false
      - key: PORT
        value: 3001
    healthCheckPath: /health
